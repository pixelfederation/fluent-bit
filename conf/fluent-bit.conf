[SERVICE]
    # Flush
    # =====
    # set an interval of seconds before to flush records to a destination
    flush        5

    # Grace
    # =====
    # Wait time (seconds) on exit
    grace        5

    # Daemon
    # ======
    # instruct Fluent Bit to run in foreground or background mode.
    daemon       Off

    # Log_Level
    # =========
    # Set the verbosity level of the service, values can be:
    #
    # - error
    # - warning
    # - info
    # - debug
    # - trace
    #
    # by default 'info' is set, that means it includes 'error' and 'warning'.
    log_level    info

    # Storage options for backpressure
    storage.path              /home/mvince/remote_projects/github/fluent/fluent-bit/storage/
    storage.sync              normal
    storage.checksum          off
    storage.backlog.mem_limit 5M


    # Parsers File
    # ============
    # specify an optional 'Parsers' configuration file
    parsers_file parsers.conf

    # Plugins File
    # ============
    # specify an optional 'Plugins' configuration file to load external plugins.
    plugins_file plugins.conf

    # HTTP Server
    # ===========
    # Enable/Disable the built-in HTTP Server for metrics
    http_server  Off
    http_listen  0.0.0.0
    http_port    2020

    # Storage
    # =======
    # Fluent Bit can use memory and filesystem buffering based mechanisms
    #
    # - https://docs.fluentbit.io/manual/administration/buffering-and-storage
    #
    # storage metrics
    # ---------------
    # publish storage pipeline metrics in '/api/v1/storage'. The metrics are
    # exported only if the 'http_server' option is enabled.
    #
    storage.metrics on

    # storage.path
    # ------------
    # absolute file system path to store filesystem data buffers (chunks).
    #
    # storage.path /tmp/storage

    # storage.sync
    # ------------
    # configure the synchronization mode used to store the data into the
    # filesystem. It can take the values normal or full.
    #
    # storage.sync normal

    # storage.checksum
    # ----------------
    # enable the data integrity check when writing and reading data from the
    # filesystem. The storage layer uses the CRC32 algorithm.
    #
    # storage.checksum off

    # storage.backlog.mem_limit
    # -------------------------
    # if storage.path is set, Fluent Bit will look for data chunks that were
    # not delivered and are still in the storage layer, these are called
    # backlog data. This option configure a hint of maximum value of memory
    # to use when processing these records.
    #
    # storage.backlog.mem_limit 5M

[INPUT]
    Name              tail
    Tag               file.nginx_access
    Path              /home/mvince/access.log
    DB                /home/mvince/nginx_access.db
    Path_Key          source
    Parser            nginx_access
    Mem_Buf_Limit     8MB
    Refresh_Interval  30

[FILTER]
    Name modify
    Match file.nginx_access
    Add es_index da-test-nginx-access


[INPUT]
    Name              tail
    Tag               file.nginx_error
    Path              /home/mvince/error.log
    DB                /home/mvince/nginx_error.db
    Path_Key          source
    Parser            nginx_error
    Mem_Buf_Limit     8MB
    Refresh_Interval  30

[FILTER]
    Name modify
    Match file.nginx_error
    Add es_index da-test-nginx-error

[INPUT]
    Name              tail
    Tag               file.fpm_error
    Path              /home/mvince/www-error.log
    DB                /home/mvince/php_error.db
    Path_Key          source
    Parser            php_error
    Mem_Buf_Limit     8MB
    Refresh_Interval  30

[FILTER]
    Name modify
    Match file.fpm_error
    Add es_index da-test-fpm_error

#[INPUT]
#    Name            zmq
#    Tag             zmq
#    Endpoint        tcp://*:9999
#    Schedule_nanos  500000
#    batch_size      10000
#    storage.type    filesystem

#[FILTER]
#    Name            lua
#    Match           zmq
#    script          fluentbit.lua
#    call            set_index


[OUTPUT]
    Name            es
    Match           *
    Host            127.0.0.1
    Port            9200
    Logstash_Prefix_Key es_index
    #Logstash_Prefix php
    Logstash_DateFormat writer
    logstash_format on
    Type            _doc
    Retry_Limit     1